<IfModule mod_rewrite.c>
    RewriteEngine On

    # Si la petición ya apunta a un archivo o carpeta real, no reescribir
    RewriteCond %{REQUEST_URI} ^/public
    RewriteRule ^(.*)$ - [L]

    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
